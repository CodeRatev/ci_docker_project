apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-app-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flask-ci-app
  template:
    metadata:
      labels:
        app: flask-ci-app
    spec:
      containers:
      - name: flask-container
        image: shotokan2003/myapp:latest
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
spec:
  type: NodePort
  selector:
    app: flask-ci-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
